import React, { useEffect, useRef, useState } from "react";
import {
  InputRightElement,
  createIcon,
  InputLeftElement,
  InputGroup,
  Flex,
  Heading,
  Input,
  useToast,
  Text
} from "@chakra-ui/react";
import { CloseIcon } from "@chakra-ui/icons";

const CardButton = () => {
  return <Flex width="100%" p={5}></Flex>;
};

export default CardButton;
